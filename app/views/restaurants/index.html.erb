<h1>All Restaurants</h1>

<%= link_to "Create a Restaurant", new_restaurant_path, class: "btn btn-success"%>
<% @restaurants.each do |restaurant| %>
  <p><%= restaurant.name %><p>
  <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary"%>
<% end %>